# The client URL that is allowed to make requests to this server.
# In production, this should be your frontend's domain (e.g., https://www.your-app.com).
# For development, it's typically the local client's address.
# Use a comma-separated list for multiple origins.
CORS_ORIGIN=http://localhost:5173,http://localhost:4173
